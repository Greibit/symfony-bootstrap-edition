{% extends 'GreibitPanelAdminBundle::layout.html.twig' %}

{% block title %}
{{ parent() }} - {{ 'views.new.creation'|trans({'%entity%': 'Test'}, 'GreibitCrudGeneratorBundle') }}
{% endblock %}

{% block page %}
<div class="row">
    <div class="col-md-12">
        <h1 class="page-header">
            {{ 'views.new.creation'|trans({'%entity%': 'Test'}, 'GreibitCrudGeneratorBundle') }}
            <span class="float-right">
                            <div class="float-left">
        <a class="btn btn-default btn-circle btn-lg" href="{{ path('test') }}">
            <span class="glyphicon glyphicon-align-justify"></span>
        </a>
    </div>
            </span>
        </h1>

        <form class="well" action="{{ path('test_create') }}" method="post" {{ form_enctype(form) }}>
            {{ form_widget(form) }}
            <p>
                <button type="submit" class="btn btn-success">{{ 'views.new.create'|trans({}, 'GreibitCrudGeneratorBundle') }}</button>
            </p>
        </form>
    </div>
</div>
{% endblock %}